<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Cross-instance shared state abstraction for Trellis — ISharedStateStore with in-memory and IDistributedCache providers. Back router circuit-breaker health (or any shared value) with the store of your choice.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Caching.Abstractions" Version="10.0.10" />
  </ItemGroup>

</Project>

